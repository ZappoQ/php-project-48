<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="PHP Project">
    <rule ref="PSR12"/>
    <file>src</file>
    <file>tests</file>
    <exclude-pattern>vendor/*</exclude-pattern>

    <!-- Игнорируем require_once в тестах -->
    <rule ref="PSR1.Files.SideEffects">
        <exclude-pattern>tests/*</exclude-pattern>
    </rule>
</ruleset>